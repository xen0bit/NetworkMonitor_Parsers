// Filters packets with low signal strength or low data rate.

// Adjust the RSSI value according to your adapters max RSSI value.
// In the example below, this is 60.  
//      This is just a  sample values based on a signal strength 
// < ~25%.  RSSI is not the best measurement since it doesn't 
// take into account quality of signal.

WiFi.MetaData.lRSSI < (60 / 4)
OR
WiFi.MetaData.Rate < 20

